define(['require', 'app'],
    function(require, APP) {
        /**
         * Controller for tabProfile generated by Appery.io
         * @module tabProfile
         */
        APP.controller('tabProfile', ['$scope', 'Apperyio', controller]);

        function controller($scope, Apperyio) {
            /**
             * user controller variables
             */
            $scope.user = Apperyio.EntityAPI('User');
            $scope.username = Apperyio.EntityAPI('String');
            $scope.LastName = Apperyio.EntityAPI('String');
            $scope.token = Apperyio.EntityAPI('String');
            $scope.profile_id = Apperyio.EntityAPI('String');
            $scope.new_email1 = Apperyio.EntityAPI('String');
            $scope.new_mobile1 = Apperyio.EntityAPI('String');
            $scope.new_password1 = Apperyio.EntityAPI('String');
            $scope.new_company1 = Apperyio.EntityAPI('String');
            $scope.new_businessphone1 = Apperyio.EntityAPI('String');
            $scope.new_shippingaddress1a = Apperyio.EntityAPI('String');
            $scope.new_shippingaddress2a = Apperyio.EntityAPI('String');
            $scope.new_firstname1 = Apperyio.EntityAPI('String');
            $scope.new_lastname1 = Apperyio.EntityAPI('String');
            $scope.new_state1 = Apperyio.EntityAPI('String');
            $scope.new_city1 = Apperyio.EntityAPI('String');
            $scope.new_resalenumber1 = Apperyio.EntityAPI('String');
            $scope.new_zip1 = Apperyio.EntityAPI('String');
            /**
             * User controller functions
             */
            /**
             * @function init
             */
            $scope.init = function() {
                //On load screen logic
                var userData = Apperyio.get("dataStorage");
                $scope.user = userData.current;
            };
            /**
             * @function lo
             */
            $scope.lo = function() {
                var requestData = {};
                var sessionToken = $scope.user.session;
                console.log(sessionToken);
                requestData = (function mapping7038($scope) {
                    var requestData = {};
                    requestData.headers = {};
                    var user_scope = $scope.user;
                    requestData.headers['X-Appery-Session-Token'] = user_scope.session;
                    return requestData;
                    /*|button_mapping|onbeforesend|74FD9B36-8A5C-B2C0-74B7-1A0F1D5F2A73||7038|*/
                })($scope);
                // read more about using rest services: https://links.appery.io/ve-snippet-rest
                Apperyio.get("phoneapp_logout_service")(requestData).then(
                    /*|service_bookmark|bookmark|74FD9B36-8A5C-B2C0-74B7-1A0F1D5F2A73||5562|*/
                    function(success) { // success callback
                        /*|button_mapping|onsuccess|74FD9B36-8A5C-B2C0-74B7-1A0F1D5F2A73||3008|*/
                        Apperyio.navigateTo("Login", {});
                    },
                    function(error) { // callback to handle request error
                        Apperyio.navigateTo("Login", {});
                    },
                    function(notify) { // notify callback, can fire few times
                    });
            };
            /**
             * @function changeEmail
             */
            $scope.changeEmail = function() {
                //get user data
                var userData = Apperyio.get("dataStorage");
                var AllData = userData.current;
                var username = AllData.username;
                var token = AllData.session;
                var profile_id = AllData.profile_id;
                var new_email1 = $scope.new_email;
                console.log(new_email1);
                //get user data
                var requestData = {};
                requestData = (function mapping4906($scope) {
                    var requestData = {};
                    requestData.params = {};
                    requestData.params.profile_id = profile_id;
                    requestData.params.changeField = new_email1;
                    requestData.params.username = username;
                    requestData.headers = {};
                    var token_scope = $scope.token;
                    requestData.headers['X-Appery-Session-Token'] = token;
                    console.log("token", token);
                    return requestData;
                    /*|button_mapping|onbeforesend|FBD35E48-B62B-B724-C2F2-E142D81C0C0A||4906|*/
                })($scope);
                // read more about using rest services: https://links.appery.io/ve-snippet-rest
                Apperyio.get("update_Profile_Email_service")(requestData).then(
                    /*|service_bookmark|bookmark|FBD35E48-B62B-B724-C2F2-E142D81C0C0A||7659|*/
                    function(success) { // success callback
                        /*|button_mapping|onsuccess|FBD35E48-B62B-B724-C2F2-E142D81C0C0A||6286|*/
                        alert('You have successfully changed your email address.  Please remember to verify your new email address.');
                        userData.current.email = new_email1;
                        userData.current.EmailVer = false;
                    },
                    function(error) { // callback to handle request error
                        alert('An error has occured. Please try again Later');
                        //Apperyio.navigateTo("Contact_Us", {}); 
                    },
                    function(notify) { // notify callback, can fire few times
                    });
            };
            /**
             * @function changeMobile
             */
            $scope.changeMobile = function() {
                //get user data
                var userData = Apperyio.get("dataStorage");
                var AllData = userData.current;
                var username = AllData.username;
                var token = AllData.session;
                var profile_id = AllData.profile_id;
                var new_mobile1 = $scope.new_MobilePhone;
                console.log(new_mobile1);
                //get user data
                var requestData = {};
                requestData = (function mapping5170($scope) {
                    var requestData = {};
                    requestData.params = {};
                    var new_mobile1_scope = $scope.new_mobile1;
                    var profile_id_scope = $scope.profile_id;
                    var username_scope = $scope.username;
                    requestData.params.changeField = new_mobile1;
                    requestData.params.profile_id = profile_id;
                    requestData.params.username = username;
                    requestData.headers = {};
                    var token_scope = $scope.token;
                    requestData.headers['X-Appery-Session-Token'] = token;
                    console.log("token", token);
                    return requestData;
                    /*|button_mapping|onbeforesend|7270039F-DFBC-407B-0E12-99211DF8DD10||5170|*/
                })($scope);
                // read more about using rest services: https://links.appery.io/ve-snippet-rest
                Apperyio.get("update_Profile_Mobile_service")(requestData).then(
                    /*|service_bookmark|bookmark|7270039F-DFBC-407B-0E12-99211DF8DD10||1323|*/
                    function(success) { // success callback
                        /*|button_mapping|onsuccess|7270039F-DFBC-407B-0E12-99211DF8DD10||3058|*/
                        alert('You have successfully changed your Mobile Number.  Please remember to verify your mobile number.');
                        userData.current.MobilePhone = "+1" + new_mobile1;
                        userData.current.TextVer = false;
                    },
                    function(error) { // callback to handle request error
                        alert('An error has occured. Please try again Later');
                        //Apperyio.navigateTo("Contact_Us", {}); 
                    },
                    function(notify) { // notify callback, can fire few times
                    });
            };
            /**
             * @function changePassword
             */
            $scope.changePassword = function() {
                //get user data
                var userData = Apperyio.get("dataStorage");
                var AllData = userData.current;
                var username = AllData.username;
                var token = AllData.session;
                var new_password1 = $scope.newPassword;
                console.log("Password", new_password1);
                //get user data
                var requestData = {};
                requestData = (function mapping1486($scope) {
                    var requestData = {};
                    requestData.params = {};
                    var new_password1_scope = $scope.new_password1;
                    var username_scope = $scope.username;
                    requestData.params.changeField = new_password1;
                    requestData.params.username = username;
                    requestData.headers = {};
                    var token_scope = $scope.token;
                    requestData.headers['X-Appery-Session-Token'] = token;
                    console.log("token", token);
                    return requestData;
                    /*|button_mapping|onbeforesend|4407D359-6BC7-B677-ED99-C0727B646280||1486|*/
                })($scope);
                // read more about using rest services: https://links.appery.io/ve-snippet-rest
                Apperyio.get("update_Profile_Password_service")(requestData).then(
                    /*|service_bookmark|bookmark|4407D359-6BC7-B677-ED99-C0727B646280||9522|*/
                    function(success) { // success callback
                        /*|button_mapping|onsuccess|4407D359-6BC7-B677-ED99-C0727B646280||8455|*/
                        alert('You have successfully changed your Password.');
                        Apperyio.navigateTo("Profile", {});
                    },
                    function(error) { // callback to handle request error
                        alert('An error has occured. Please try again Later');
                        Apperyio.navigateTo("Contact_Us", {});
                    },
                    function(notify) { // notify callback, can fire few times
                    });
            };
            /**
             * @function changeCompany
             */
            $scope.changeCompany = function() {
                //get user data
                var userData = Apperyio.get("dataStorage");
                var AllData = userData.current;
                var username = AllData.username;
                var token = AllData.session;
                var profile_id = AllData.profile_id;
                var new_company1 = $scope.new_Company;
                console.log(new_company1);
                //get user data
                var requestData = {};
                requestData = (function mapping8530($scope) {
                    var requestData = {};
                    requestData.params = {};
                    //var new_company1_scope = $scope.new_company1;
                    //var profile_id_scope = $scope.profile_id;
                    requestData.params.changeField = new_company1;
                    requestData.params.profile_id = profile_id;
                    requestData.headers = {};
                    var token_scope = $scope.token;
                    requestData.headers['X-Appery-Session-Token'] = token;
                    console.log("token", token);
                    return requestData;
                    /*|button_mapping|onbeforesend|57BC03D7-0C2F-BACA-4562-297B4AF69C2D||8530|*/
                })($scope);
                // read more about using rest services: https://links.appery.io/ve-snippet-rest
                Apperyio.get("update_Profile_Company_service")(requestData).then(
                    /*|service_bookmark|bookmark|57BC03D7-0C2F-BACA-4562-297B4AF69C2D||4132|*/
                    function(success) { // success callback
                        /*|button_mapping|onsuccess|57BC03D7-0C2F-BACA-4562-297B4AF69C2D||4731|*/
                        alert('You have successfully changed your Company Name.');
                        var requestData = {};
                        var sessionToken = $scope.user.session;
                        userData.current.Company = new_company1;
                    },
                    function(error) { // callback to handle request error
                        alert('An error has occured. Please try again Later');
                    },
                    function(notify) { // notify callback, can fire few times
                    });
            };
            /**
             * @function changeBusinessPhone
             */
            $scope.changeBusinessPhone = function() {
                //get user data
                var userData = Apperyio.get("dataStorage");
                var AllData = userData.current;
                var username = AllData.username;
                var token = AllData.session;
                var profile_id = AllData.profile_id;
                var new_businessphone1 = $scope.New_BusinessPhone;
                console.log(new_businessphone1);
                //get user data
                var requestData = {};
                requestData = (function mapping8530($scope) {
                    var requestData = {};
                    requestData.params = {};
                    //var new_company1_scope = $scope.new_company1;
                    //var profile_id_scope = $scope.profile_id;
                    requestData.params.changeField = new_businessphone1;
                    requestData.params.profile_id = profile_id;
                    requestData.headers = {};
                    var token_scope = $scope.token;
                    requestData.headers['X-Appery-Session-Token'] = token;
                    console.log("token", token);
                    return requestData;
                    /*CLICK TO EDIT MAPPING*/
                })($scope);
                // read more about using rest services: https://links.appery.io/ve-snippet-rest
                Apperyio.get("update_Profile_BusinessPhone_service")(requestData).then(
                    function(success) { // success callback
                        /*CLICK TO EDIT MAPPING*/
                        alert('You have successfully changed your Business Phone Number.');
                        userData.current.BusinessPhone = new_businessphone1;
                    },
                    function(error) { // callback to handle request error
                        alert('An error has occured. Please try again Later');
                        //Apperyio.navigateTo("Contact_Us", {}); 
                    },
                    function(notify) { // notify callback, can fire few times
                    });
            };
            /**
             * @function changeShippingAddress1
             */
            $scope.changeShippingAddress1 = function() {
                //get user data
                var userData = Apperyio.get("dataStorage");
                var AllData = userData.current;
                var username = AllData.username;
                var token = AllData.session;
                var profile_id = AllData.profile_id;
                var new_shippingaddress1a = $scope.New_ShippingAddress1;
                console.log(new_shippingaddress1a);
                //get user data
                var requestData = {};
                requestData = (function mapping8530($scope) {
                    var requestData = {};
                    requestData.params = {};
                    //var new_company1_scope = $scope.new_company1;
                    //var profile_id_scope = $scope.profile_id;
                    requestData.params.changeField = new_shippingaddress1a;
                    requestData.params.profile_id = profile_id;
                    requestData.headers = {};
                    var token_scope = $scope.token;
                    requestData.headers['X-Appery-Session-Token'] = token;
                    console.log("token", token);
                    return requestData;
                    /*CLICK TO EDIT MAPPING*/
                })($scope);
                // read more about using rest services: https://links.appery.io/ve-snippet-rest
                Apperyio.get("update_Profile_ShippingAddress1_service")(requestData).then(
                    function(success) { // success callback
                        /*CLICK TO EDIT MAPPING*/
                        alert('You have successfully changed your Shipping Address.');
                        userData.current.ShippingAddress1 = new_shippingaddress1a;
                    },
                    function(error) { // callback to handle request error
                        alert('An error has occured. Please try again Later');
                        //Apperyio.navigateTo("Contact_Us", {}); 
                    },
                    function(notify) { // notify callback, can fire few times
                    });
            };
            /**
             * @function changeShippingAddress2
             */
            $scope.changeShippingAddress2 = function() {
                //get user data
                var userData = Apperyio.get("dataStorage");
                var AllData = userData.current;
                var username = AllData.username;
                var token = AllData.session;
                var profile_id = AllData.profile_id;
                var new_shippingaddress2a = $scope.New_ShippingAddress2;
                console.log(new_shippingaddress2a);
                //get user data
                var requestData = {};
                requestData = (function mapping8530($scope) {
                    var requestData = {};
                    requestData.params = {};
                    //var new_company1_scope = $scope.new_company1;
                    //var profile_id_scope = $scope.profile_id;
                    requestData.params.changeField = new_shippingaddress2a;
                    requestData.params.profile_id = profile_id;
                    requestData.headers = {};
                    var token_scope = $scope.token;
                    requestData.headers['X-Appery-Session-Token'] = token;
                    console.log("token", token);
                    return requestData;
                    /*CLICK TO EDIT MAPPING*/
                })($scope);
                // read more about using rest services: https://links.appery.io/ve-snippet-rest
                Apperyio.get("update_Profile_ShippingAddress2_service")(requestData).then(
                    function(success) { // success callback
                        /*CLICK TO EDIT MAPPING*/
                        alert('You have successfully changed your Unit / Suite #.');
                        userData.current.ShippingAddress2 = new_shippingaddress2a;
                    },
                    function(error) { // callback to handle request error
                        alert('An error has occured. Please try again Later');
                        Apperyio.navigateTo("Contact_Us", {});
                    },
                    function(notify) { // notify callback, can fire few times
                    });
            };
            /**
             * @function changeFirstName
             */
            $scope.changeFirstName = function() {
                //get user data
                var userData = Apperyio.get("dataStorage");
                var AllData = userData.current;
                var username = AllData.username;
                var token = AllData.session;
                var profile_id = AllData.profile_id;
                var new_firstname1 = $scope.new_FirstName;
                console.log(new_firstname1);
                //get user data
                var requestData = {};
                requestData = (function mapping8530($scope) {
                    var requestData = {};
                    requestData.params = {};
                    //var new_company1_scope = $scope.new_company1;
                    //var profile_id_scope = $scope.profile_id;
                    requestData.params.changeField = new_firstname1;
                    requestData.params.profile_id = profile_id;
                    requestData.headers = {};
                    var token_scope = $scope.token;
                    requestData.headers['X-Appery-Session-Token'] = token;
                    console.log("token", token);
                    return requestData;
                    /*CLICK TO EDIT MAPPING*/
                })($scope);
                // read more about using rest services: https://links.appery.io/ve-snippet-rest
                Apperyio.get("update_Profile_FirstName_service")(requestData).then(
                    function(success) { // success callback
                        /*CLICK TO EDIT MAPPING*/
                        alert('You have successfully changed your First Name.');
                        userData.current.FirstName = new_firstname1;
                    },
                    function(error) { // callback to handle request error
                        alert('An error has occured. Please try again Later');
                        Apperyio.navigateTo("Contact_Us", {});
                    },
                    function(notify) { // notify callback, can fire few times
                    });
            };
            /**
             * @function changeLastName
             */
            $scope.changeLastName = function() {
                //get user data
                var userData = Apperyio.get("dataStorage");
                var AllData = userData.current;
                var username = AllData.username;
                var token = AllData.session;
                var profile_id = AllData.profile_id;
                var new_lastname1 = $scope.new_LastName;
                console.log(new_lastname1);
                //get user data
                var requestData = {};
                requestData = (function mapping8530($scope) {
                    var requestData = {};
                    requestData.params = {};
                    //var new_company1_scope = $scope.new_company1;
                    //var profile_id_scope = $scope.profile_id;
                    requestData.params.changeField = new_lastname1;
                    requestData.params.profile_id = profile_id;
                    requestData.headers = {};
                    var token_scope = $scope.token;
                    requestData.headers['X-Appery-Session-Token'] = token;
                    console.log("token", token);
                    return requestData;
                    /*CLICK TO EDIT MAPPING*/
                })($scope);
                // read more about using rest services: https://links.appery.io/ve-snippet-rest
                Apperyio.get("update_Profile_LastName_service")(requestData).then(
                    function(success) { // success callback
                        /*CLICK TO EDIT MAPPING*/
                        alert('You have successfully changed your Last Name.');
                        userData.current.LastName = new_lastname1;
                    },
                    function(error) { // callback to handle request error
                        alert('An error has occured. Please try again Later');
                        //Apperyio.navigateTo("Contact_Us", {}); 
                    },
                    function(notify) { // notify callback, can fire few times
                    });
            };
            /**
             * @function changeState
             */
            $scope.changeState = function() {
                //get user data
                var userData = Apperyio.get("dataStorage");
                var AllData = userData.current;
                var username = AllData.username;
                var token = AllData.session;
                var profile_id = AllData.profile_id;
                var new_state1 = $scope.New_State;
                console.log(new_state1);
                //get user data
                var requestData = {};
                requestData = (function mapping8530($scope) {
                    var requestData = {};
                    requestData.params = {};
                    //var new_company1_scope = $scope.new_company1;
                    //var profile_id_scope = $scope.profile_id;
                    requestData.params.changeField = new_state1;
                    requestData.params.profile_id = profile_id;
                    requestData.headers = {};
                    var token_scope = $scope.token;
                    requestData.headers['X-Appery-Session-Token'] = token;
                    console.log("token", token);
                    return requestData;
                    /*CLICK TO EDIT MAPPING*/
                })($scope);
                // read more about using rest services: https://links.appery.io/ve-snippet-rest
                Apperyio.get("update_Profile_State_service")(requestData).then(
                    function(success) { // success callback
                        /*CLICK TO EDIT MAPPING*/
                        alert('You have successfully changed your State.');
                        userData.current.State = new_state1;
                    },
                    function(error) { // callback to handle request error
                        alert('An error has occured. Please try again Later');
                        //Apperyio.navigateTo("Contact_Us", {}); 
                    },
                    function(notify) { // notify callback, can fire few times
                    });
            };
            /**
             * @function changeCity
             */
            $scope.changeCity = function() {
                //get user data
                var userData = Apperyio.get("dataStorage");
                var AllData = userData.current;
                var username = AllData.username;
                var token = AllData.session;
                var profile_id = AllData.profile_id;
                var new_city1 = $scope.New_City;
                console.log(new_city1);
                //get user data
                var requestData = {};
                requestData = (function mapping8530($scope) {
                    var requestData = {};
                    requestData.params = {};
                    //var new_company1_scope = $scope.new_company1;
                    //var profile_id_scope = $scope.profile_id;
                    requestData.params.changeField = new_city1;
                    requestData.params.profile_id = profile_id;
                    requestData.headers = {};
                    var token_scope = $scope.token;
                    requestData.headers['X-Appery-Session-Token'] = token;
                    console.log("token", token);
                    return requestData;
                    /*CLICK TO EDIT MAPPING*/
                })($scope);
                // read more about using rest services: https://links.appery.io/ve-snippet-rest
                Apperyio.get("update_Profile_City_service")(requestData).then(
                    function(success) { // success callback
                        /*CLICK TO EDIT MAPPING*/
                        alert('You have successfully changed your City.');
                        userData.current.City = new_city1;
                    },
                    function(error) { // callback to handle request error
                        alert('An error has occured. Please try again Later');
                        //Apperyio.navigateTo("Contact_Us", {}); 
                    },
                    function(notify) { // notify callback, can fire few times
                    });
            };
            /**
             * @function changeResale
             */
            $scope.changeResale = function() {
                //get user data
                var userData = Apperyio.get("dataStorage");
                var AllData = userData.current;
                var username = AllData.username;
                var token = AllData.session;
                var profile_id = AllData.profile_id;
                var new_resalenumber1 = $scope.New_Resale;
                console.log(new_resalenumber1);
                //get user data
                var requestData = {};
                requestData = (function mapping8530($scope) {
                    var requestData = {};
                    requestData.params = {};
                    //var new_company1_scope = $scope.new_company1;
                    //var profile_id_scope = $scope.profile_id;
                    requestData.params.changeField = new_resalenumber1;
                    requestData.params.profile_id = profile_id;
                    requestData.headers = {};
                    var token_scope = $scope.token;
                    requestData.headers['X-Appery-Session-Token'] = token;
                    console.log("token", token);
                    return requestData;
                    /*CLICK TO EDIT MAPPING*/
                })($scope);
                // read more about using rest services: https://links.appery.io/ve-snippet-rest
                Apperyio.get("update_Profile_Resale_service")(requestData).then(
                    function(success) { // success callback
                        /*CLICK TO EDIT MAPPING*/
                        alert('You have successfully changed your Resale Number.');
                        userData.current.ResaleNumber = new_resalenumber1;
                    },
                    function(error) { // callback to handle request error
                        alert('An error has occured. Please try again Later');
                        //Apperyio.navigateTo("Contact_Us", {}); 
                    },
                    function(notify) { // notify callback, can fire few times
                    });
            };
            /**
             * @function changeZip
             */
            $scope.changeZip = function() {
                //get user data
                var userData = Apperyio.get("dataStorage");
                var AllData = userData.current;
                var username = AllData.username;
                var token = AllData.session;
                var profile_id = AllData.profile_id;
                var new_zip1 = $scope.New_Zip;
                console.log(new_zip1);
                //get user data
                var requestData = {};
                requestData = (function mapping8530($scope) {
                    var requestData = {};
                    requestData.params = {};
                    //var new_company1_scope = $scope.new_company1;
                    //var profile_id_scope = $scope.profile_id;
                    requestData.params.changeField = new_zip1;
                    requestData.params.profile_id = profile_id;
                    requestData.headers = {};
                    var token_scope = $scope.token;
                    requestData.headers['X-Appery-Session-Token'] = token;
                    console.log("token", token);
                    return requestData;
                    /*CLICK TO EDIT MAPPING*/
                })($scope);
                // read more about using rest services: https://links.appery.io/ve-snippet-rest
                Apperyio.get("update_Profile_Zip_service")(requestData).then(
                    function(success) { // success callback
                        /*CLICK TO EDIT MAPPING*/
                        alert('You have successfully changed your Zip Code.');
                        userData.current.Zip = new_zip1;
                    },
                    function(error) { // callback to handle request error
                        alert('An error has occured. Please try again Later');
                        //Apperyio.navigateTo("Contact_Us", {}); 
                    },
                    function(notify) { // notify callback, can fire few times
                    });
            };
        }
    });